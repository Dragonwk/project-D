using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fileText : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        TextAsset asset;

        void Start()
        {
            print(asset.text);
        }
    }
}
